rules_version = '2';

// 나만 보기: 로그인한 본인(uid)만 자기 기록을 읽고 쓸 수 있음.
// 다른 사람은 읽기도, 쓰기도 불가능.
service cloud.firestore {
  match /databases/{database}/documents {
    match /users/{uid}/entries/{entryId} {
      allow read, write: if request.auth != null && request.auth.uid == uid;
    }
  }
}
